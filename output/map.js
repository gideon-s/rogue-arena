// Generated by CoffeeScript 1.7.1
(function() {
  window.Map = (function() {
    function Map() {
      this.squares = {};
      console.log("Being Constructed, yo");
    }

    Map.prototype.fromPair = function(pair) {
      return pair[0] + "," + pair[1];
    };

    Map.prototype.setSquare = function(pair, symbol) {
      return this.squares[this.fromPair(pair)] = symbol;
    };

    Map.prototype.at = function(pair) {
      return this.squares[this.fromPair(pair)];
    };

    Map.prototype.locationKeys = function() {
      return Object.keys(this.squares);
    };

    Map.prototype.isOpen = function(pair) {
      return this.at(pair) != null;
    };

    Map.prototype.randomLocation = function() {
      return Coordinates.selectRandom(this.locationKeys());
    };

    return Map;

  })();

  window.NewMap = (function() {
    function NewMap(width, height) {
      var x;
      this.map = new Array(width);
      this.map = (function() {
        var _i, _results;
        _results = [];
        for (x = _i = 0; 0 <= width ? _i < width : _i > width; x = 0 <= width ? ++_i : --_i) {
          _results.push(this.map[x] = new Array(height));
        }
        return _results;
      }).call(this);
    }

    NewMap.prototype.at = function(pair) {
      return this.map[pair[0]][pair[1]];
    };

    NewMap.prototype.setLocation = function(pair, symbol) {
      return this.map[pair[0]][pair[1]] = symbol;
    };

    NewMap.prototype.isOpen = function(pair) {
      return this.at(pair) != null;
    };

    NewMap.prototype.locationKeys = function() {
      return typeof Object.keys === "function" ? Object.keys(this.map) : void 0;
    };

    NewMap.prototype.randomLocation = function() {
      return Coordinates.selectRandom(this.locationKeys());
    };

    return NewMap;

  })();

}).call(this);
