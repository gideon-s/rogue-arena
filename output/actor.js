// Generated by CoffeeScript 1.7.1
(function() {
  window.Actor = (function() {
    function Actor(location, sigil, color, speed) {
      this.location = location;
      this.sigil = sigil;
      this.color = color;
      this.speed = speed;
      this.dead = false;
      this.action();
    }

    Actor.prototype.action = function() {
      Game.drawMapLocation(this.location);
      if ((Game.player != null) && Game.player.dead) {
        return;
      }
      if (this.dead) {
        Game.died(this);
        return;
      }
      this.act();
      if (!Game.map.isOpen(this.location)) {
        this.dead = true;
        return;
      }
      Game.enters(this.location, this);
      this.draw();
      return window.setTimeout(((function(_this) {
        return function() {
          return _this.action();
        };
      })(this)), this.speed);
    };

    Actor.prototype.died = function() {};

    Actor.prototype.draw = function() {
      return Game.draw(this.location, this.sigil, this.color);
    };

    Actor.prototype.destroy = function() {
      return this.dead = true;
    };

    return Actor;

  })();

}).call(this);
